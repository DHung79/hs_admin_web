import 'upload_image.dart';

class MediaPicker {
  MediaPicker._();
  static pickMedia({Function(List<UploadImage>)? onCompleted, bool isMultiple = false,}) {
    throw 'Platform Not Supported';
  }
}
